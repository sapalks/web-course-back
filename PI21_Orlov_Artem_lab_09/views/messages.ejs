<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css">
    <title>Чат программа</title>
    <style>
        body {
            background: #fcfcfc;
        }
    </style>
</head>
<body>
<div class="container">
    <div class="py-5 text-center">
        <h2>Чат программа</h2>
        <p class="lead">Укажите ваше имя и начинайте переписку</p>
        <p class="lead">Канал: <b><%= channel %></b></p>
    </div>
    <div class="row">
        <div class="col-6">
            <h3>Форма сообщений</h3>
            <form id="messForm">
                <label id="name_label" for="name">Имя</label>
                <label id="bad_name" class="text-danger" for="name" style="display: none;">Пользователь с таким ником
                    уже существует</label>
                <input type="text" name="name" id="name" placeholder="Введите имя" class="form-control">
                <br>
                <label for="message">Сообщение</label>
                <textarea name="message" id="message" class="form-control" placeholder="Введите сообщение"></textarea>
                <br>
                <input type="submit" value="Отправить" class="btn btn-primary">
            </form>
        </div>
        <dic class="col-6">
            <h3>Сообщения</h3>
            <div id="all_mess">
            </div>
        </dic>
    </div>
</div>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
<script src="/socket.io/socket.io.js"></script>
<script>
    $(function () {
        //const socket = io.join();
        const socket = io({
            query: {
                channel: '<%= channel %>'
            }
        });
        const $form = $("#messForm");
        const $name = $("#name");
        const nameLabel = $("#name_label");
        const badNameLabel = $("#bad_name");
        const $textarea = $("#message");
        const $all_messages = $("#all_mess");

        $form.submit(function (event) {
            event.preventDefault();
            socket.emit('send mess', {
                mess: $textarea.val(),
                name: $name.val(),
                className: 'info',
                channel: '<%= channel %>'
            });
            $textarea.val('');
        });

        socket.on('add mess', function (data) {
            nameLabel.removeAttr('style');
            badNameLabel.attr('style', 'display: none;');
            $all_messages.append("<div class='alert alert-" + data.className + "'><b>" + data.name + "</b>: " + data.mess + "</div>");
        });

        socket.on('bad name', (data) => {
            if (socket.id !== data.id) {
                return;
            }
            nameLabel.attr('style', 'display: none;');
            badNameLabel.removeAttr('style');
        });
    });
</script>
</body>
</html>